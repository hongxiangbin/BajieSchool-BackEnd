<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zlfinfo.mapper.UserMapper">

    <select id="findById" resultType="com.zlfinfo.model.User" parameterType="java.lang.Long">
        SELECT * FROM user WHERE id = #{0}
    </select>

    <select id="findAll" resultType="com.zlfinfo.model.User">
        SELECT * FROM user ORDER BY id DESC
    </select>

    <insert id="insert" parameterType="com.zlfinfo.model.User">
        <selectKey resultType="java.lang.Long" keyProperty="id" order="AFTER">
            SELECT LAST_INDEX_ID()
        </selectKey>
        INSERT INTO user (username,password)
        VALUES (#{username,jdbcType=VARCHAR},#{password,jdbcType=VARCHAR})
    </insert>

    <update id="update" parameterType="com.zlfinfo.model.User">
        UPDATE user
        <set>
            <if test="username!=null">
                username = #{username,jdbcType=VARCHAR}
            </if>
            <if test="password!=null">
                password = #{password,jdbcType=VARCHAR}
            </if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM user
        WHERE id = ##{id,jdbcType=BIGINT}
    </delete>
</mapper>